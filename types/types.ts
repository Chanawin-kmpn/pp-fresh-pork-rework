import { ReactNode } from "react";

export interface CertificateProp {
  icon: string;
  title: string;
  desc: string;
}

export interface Initiative {
  icon: string;
  title: string;
  desc: string;
}
